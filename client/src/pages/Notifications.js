import styled from "styled-components";

const Notifications = () => {
    return (
        <Notification>
            <h1>Notification</h1>
        </Notification>
    );
}

const Notification = styled.div`
    position: fixed;
    left: 300px;
    
`

export default Notifications;