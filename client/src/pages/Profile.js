import styled from "styled-components";

const Profile = () => {
    return (
        <ProfileTitle>
            <h1>Profile</h1>
        </ProfileTitle>
    );
}

const ProfileTitle = styled.div`
    position: fixed;
    left: 300px;
    
`
export default Profile;